apiVersion: v1
kind: Service
metadata:
  name: argus
  namespace: argus
  labels:
    app: argus
spec:
  selector:
    app: argus
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
